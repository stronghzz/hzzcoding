#include "Tree.h"

int main() {

	//while (1) {
		//int r = scanf("%s", preorder);
	/*
		if (r == EOF) {
			break;
		}
		*/
		//int preorder[] = {1, 2, 4, '#', '#', '#', 3};
	//int preorder[] = { 1,2,'#',4,'#','#',3 };
	//int size = sizeof(preorder) / sizeof(preorder[0]);
	//Result tree = CreateTree(preorder, size);
	//Preorder(tree.root);
	//printf("\n");
	//Inorder(tree.root);


	//memset(buff, 0x0, 100 * 10000);
	//index = 0;
	//PreorderBracket(result.root);

	//buff[index - 1] = 0;
	//printf("%s\n", buff + 1);


	/*
	printf("前序遍历: ");
	PreorderTraversalNoR(result.root);
	printf("\n");

	printf("中序遍历: ");
	InorderTraversalNoR(result.root);
	printf("\n");

	printf("后序遍历: ");
	PostorderTraversalNoR(result.root);
	printf("\n");
	*/

	/*
	printf("层序遍历: ");
	LevelOrderTraversal(result.root);
	printf("\n");
	*/

	/*
	Inorder(result.root);

	n = 0;
	GetSize(result.root);
	printf("\n");

	printf("结点个数: %d\n", n);
	leaf_n = 0;
	GetLeafSize(result.root);
	printf("叶子结点个数: %d\n", leaf_n);

	int leaf_n2 = GetLeafSize2(result.root);
	printf("2 叶子结点个数: %d\n", leaf_n2);
	*/
	//}

		//sum(10 * 0000 * 0000); 

	return 0;
}